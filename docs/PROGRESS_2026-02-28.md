# Progress — Feb 28

## iOS: Third-Party App Auto-Return
- `proc_pidpath` fails on iOS (both extension and main app) — sandbox blocks it
- `_hostApplicationBundleIdentifier` returns `<null>` on iOS 18
- `_hostProcessIdentifier` works — returns valid host PID
- **New approach (deployed, untested)**: `sysctl(KERN_PROC_PID)` → process name → match installed apps via `LSApplicationWorkspace.allApplications()`, 3-pass matching (exact → prefix → CFBundleExecutable)

## macOS: Volcano Streaming
- Investigated "streaming not working" — credentials, settings, endpoint all verified correct
- Root cause: stale app binary. Fresh build fixed it
- User confirmed: "现在是可以实时输出了"
- Added UserDefaults-based diagnostics for sandboxed app debugging

## Test Scripts
- `/tmp/test_decrypt.swift` — EmbeddedKeyProvider decryption verification
- `/tmp/test_volcano_ws.swift` — Volcano WebSocket connectivity test

## RemoteLogger
- UDP wireless logging: `rlog("msg")` → Mac (192.168.86.40:9877) → `/tmp/echo_logs.txt`

*Branch: `feature/rebuild-from-0225` | Tag: `checkpoint-0228`*
